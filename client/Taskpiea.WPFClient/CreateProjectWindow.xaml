<Window x:Class="Taskpiea.WPFClient.CreateProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Taskpiea.WPFClient"
        mc:Ignorable="d"
        Title="CreateProjectWindow" Height="400" Width="400"
        WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"  />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"  />
        </Grid.RowDefinitions>

        <Border Height="30" MouseDown="Border_MouseDown" Background="Transparent">
            <StackPanel Orientation="Horizontal">
                <Button Name="CloseButton" Content="🗙" Click="CloseButton_Click" />
            </StackPanel>
        </Border>
        
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="Create Project" FontSize="22" Margin="15" />
            <TextBox Name="NameTextBox" Width="150" Margin="5"/>
            <Button Name="CreateButton" Content="Create" Click="CreateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
